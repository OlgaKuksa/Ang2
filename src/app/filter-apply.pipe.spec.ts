import { FilterApplyPipe } from './filter-apply.pipe';

describe('FilterApplyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterApplyPipe();
    expect(pipe).toBeTruthy();
  });
});
